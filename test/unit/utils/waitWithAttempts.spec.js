describe('waitWithAttempts', () => {
  it('should sequentially execute function selected number of attempts waiting for result');
  it('should exit early if function returns a result');

  it('should throw an error if function throws an error');
});
